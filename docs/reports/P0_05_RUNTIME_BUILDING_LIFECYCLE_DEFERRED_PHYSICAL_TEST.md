# P0-05 运行时建筑生命周期实体测试

## 当前状态

- 当前状态：`ACCEPTED`
- 测试日期：2026-07-25
- 完成项目：16 / 16
- 用户反馈：“上面的我都测试了，暂时没发现什么问题。”
- 本记录只确认用户完成了此前列出的全部测试及其总体结论，不补写用户未提供的逐项观察细节。
- 自动验证已经检查脚本、场景、记录、节点、占用格和输入分流；它不能证明真实鼠标操作的手感、可见反馈或窗口行为。
- 本清单不要求用户检查内部 `placement_id`，也不要求阅读日志、打开终端或运行命令。

## 测试准备

1. 使用当前 Godot 4.5.1 运行窗口。
2. 若画面上存在绿色或红色建造预览，先按 `Esc` 或右键退出建造模式。
3. 确认底部仍显示“建造”，地图上至少有两栋可选的“测试建筑”。
4. 若不足两栋，点击“建造”在两个不同的安全位置各放置一栋，再按 `Esc` 或右键退出建造模式。
5. 从第 1 项按顺序测试；任何一项失败立即停止，不继续确认删除或进入下一阶段。

## 失败记录格式

每个失败只需记录：

```text
失败项目：第 N 项
实际操作：
预期：
实际看到：
是否可以稳定复现：是 / 否
截图或录屏：有 / 无
```

不要根据自动测试或内部日志推断失败原因。

## 16 项集中实体测试

| # | 操作 | 预期结果 | 失败时停止位置 |
|---|---|---|---|
| 1 | 单击第一栋测试建筑。 | 建筑出现琥珀色描边，右侧显示建筑详情和“移除建筑（原型）”。 | 不进入移除确认。 |
| 2 | 点击“移除建筑（原型）”。 | 面板切换为确认状态；建筑、描边和地图内容均未消失。 | 不点击确认。 |
| 3 | 点击确认状态中的“取消”。 | 返回原建筑详情；建筑仍存在并保持选中。 | 停止，不继续删除。 |
| 4 | 再次进入确认状态并按 `Esc`。 | 只退出确认并返回原详情；建筑不消失。 | 停止。 |
| 5 | 再次进入确认状态并点击右上角关闭按钮。 | 面板和描边关闭；建筑不被删除。 | 停止。 |
| 6 | 重新选中第一栋、进入确认，再单击地图空白。 | 选择、描边和面板关闭；建筑不被删除。 | 停止。 |
| 7 | 重新选中第一栋、进入确认，再单击第二栋建筑。 | 切换为第二栋的普通详情；第一栋和第二栋都未被删除。 | 停止。 |
| 8 | 选中任一建筑、进入确认，再点击底部“建造”。 | 确认和选择消失，进入正常建造预览；原建筑不被删除。随后按 `Esc` 或右键退出建造。 | 停止。 |
| 9 | 重新选中第一栋，进入确认并点击“确认移除”。 | 只有第一栋消失；第二栋仍在。 | 不进行重建。 |
| 10 | 观察删除后的状态。 | 琥珀色描边和右侧面板均消失，没有透明面板残留。 | 停止。 |
| 11 | 单击第二栋建筑。 | 第二栋仍能正常选中并显示正确详情。 | 停止。 |
| 12 | 点击“建造”，把预览移到刚删除建筑的原位置。 | 原位置不再提示“位置已占用”，有效位置显示可放置反馈。 | 不确认放置。 |
| 13 | 在原位置左键确认放置。 | 使用普通建造流程成功重新放置测试建筑；没有“恢复”或专用重建按钮。随后退出建造模式。 | 停止。 |
| 14 | 单击刚重建的建筑。 | 新建筑可正常选中，描边和详情面板正常。 | 停止。 |
| 15 | 进入移除确认，把鼠标放在确认面板内滚轮缩放，并尝试左／中／右键拖动。 | 地图位置和缩放不变化；确认面板保持固定且不穿透。完成后点击“取消”。 | 停止。 |
| 16 | 在删除、重建后继续测试地图拖动、滚轮缩放、再次建造、重复占用拒绝、建筑选择和取消。 | 原有导航、建造和选择闭环均保持正常，没有残留确认状态或不可点击区域。 | 记录首次回归点并停止。 |

## 接受门槛

只有同时满足以下条件，P0-05 才能进入正式收尾：

1. 16 项全部由用户使用实体鼠标完成。
2. 没有目标外建筑被误删。
3. 取消、`Esc`、关闭、空白单击、切换建筑和进入建造均不会删除建筑。
4. 确认移除后原位置确实能通过普通建造流程重新使用。
5. 地图拖动、滚轮缩放、建造和选择没有回归。
6. 用户明确回复可以接受 P0-05。

用户已于 2026-07-25 完成全部 16 项测试并反馈暂未发现问题，因此本轮满足接受门槛。

## 结果记录

```text
实际测试日期：2026-07-25
完成项目：16 / 16
失败项目：用户未报告
用户结论：全部测试完成，暂未发现问题
最终状态：ACCEPTED
```

## Verdict

`ACCEPTED`
